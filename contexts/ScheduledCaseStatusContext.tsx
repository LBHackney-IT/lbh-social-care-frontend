import { createContext } from 'react';

export const ScheduledCaseStatusContext = createContext({
  scheduledPersonIdContext: 44,
  scheduledCaseContext: true,
});

export default ScheduledCaseStatusContext;
